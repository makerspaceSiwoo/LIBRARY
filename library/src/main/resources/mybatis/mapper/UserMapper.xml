<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lib.mapper.UserMapper">
    <select id="getUserCredentials" resultType="com.lib.dto.UserDto">
        select * from user
    </select>
</mapper>